# Git 常用命令

```bash
git init
git status
git add .
git commit -m "add didi api test project"
git log
git branch
git remote add origin <repo-url>
git push -u origin main
```

## 提交建议

1. 先提交项目结构。
2. 再提交 mock 服务。
3. 再提交登录测试。
4. 再提交订单测试。
5. 最后提交 README 和文档。
